Fetched: Free Universal Primers - GENEWIZ from Azenta Life SciencesFetched: Free Universal Primers - GENEWIZ from Azenta Life SciencesGot all 29 primers. Now let me read the frontend design skill before building this.Viewed a file, created a file, read a fileViewed a file, created a file, read a fileHere's your Primer Checker tool! It includes all 29 GENEWIZ universal primers and has these features:
Search capabilities:

Detects exact forward-strand matches
Optionally checks for reverse complement matches (toggle on/off)
Reports all match positions (1-based) with base ranges

Results display:

Annotated sequence view — matched regions are highlighted in green (forward) or amber (reverse complement), hover to see which primer
Per-primer cards showing match status, positions, and the matched sequence
Filter chips to show all / matches only / forward only / RC only
A "hide non-matches" toggle to reduce clutter

Future database support:

The database selector UI is already in place — when you're ready to add new primer sets, just add a new entry to the DATABASES object in the script and a <button> in the #dbOptions div, and it'll slot right in with no other changes needed.

Shortcut: Ctrl+Enter (or Cmd+Enter) runs the search from the textarea.